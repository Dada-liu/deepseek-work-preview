# Tauri+DSH 桌面Agent React前端开发实施文档（通信方案落地版）

## 一、文档目的与适用范围

本文档为 **Tauri v2 + DSH(Cordis) 桌面AI Agent** 前端开发专属实施手册，面向React技术栈开发者，明确核心通信方案选型、数据交互规范、业务适配逻辑、边界处理规则，可直接作为开发、联调、自测的唯一参考标准。

文档核心解决问题：

- 明确DSH桌面端**流式任务、长会话、实时日志**最优通信协议选型

- 统一React前端与DSH后台、Tauri管控层的交互规范

- 定义重连、断网、后台保活、任务中断、日志回溯等边界逻辑

- 适配桌面端独有特性：托盘驻留、后台长任务、离线缓存、进程重启恢复

适用技术栈：React 18 + Vite + Tauri v2 + DSH 官方原生服务

## 二、核心通信方案最终选型（硬性开发规范）

### 2.1 协议选型结论

**全局唯一推荐、强制规范：全程使用 SSE（Server-Sent Events）流式通信**

所有DSH Agent任务、实时日志、步骤推送、会话状态更新，统一通过 **HTTP SSE** 实现，为项目唯一流式通信标准。

### 2.2 选型核心依据（贴合DSH架构与桌面场景）

#### 1）DSH官方原生能力匹配

DeepSeek DSH 内置HTTP服务，原生支持SSE流式输出，所有Agent的 turn/step、工具调用、日志回放、任务状态变更均以SSE事件流对外暴露，无需后端改造、零侵入内核，完美兼容官方迭代。

#### 2）桌面Agent业务特性适配

DSH属于**服务端主动推送、客户端被动监听**的长任务场景：Agent自主循环执行、分步推送日志、无客户端高频双向交互，SSE单工流式模型完全匹配业务。

SSE单工流式模型完全匹配该业务场景，是适配DSH桌面Agent的最优流式通信方案。

#### 3）稳定性与容错性优势

- SSE 内置断线自动重连、状态记忆、事件ID回溯，天然适配桌面端后台驻留、进程重启、窗口隐藏场景

- HTTP协议可穿透本地端口校验、防火墙拦截

- Tauri沙盒环境对SSE支持更友好，无WS握手超时、连接劫持问题

## 三、整体通信架构（React + Tauri + DSH）

### 3.1 分层通信链路

```plain
React前端页面
  ↓ 普通HTTP POST/GET（下发任务、查询配置、保存参数）
Tauri 本地端口服务 127.0.0.1:XXXX
  ↓ SSE长连接（持续推送Agent实时事件流）
DSH Cordis 后台运行时
  ↓ 本地工具执行 / 云端模型推理
返回流式结果、步骤日志、任务状态、KV缓存状态
```

### 3.2 分工规范（严格执行）

- **HTTP 短请求**：负责一次性交互，如创建会话、提交任务、修改配置、切换模型、清空日志

- **SSE 长连接**：负责持续性流式推送，如Agent步骤、工具调用记录、实时日志、任务进度、完成状态、报错信息

- **Tauri IPC**：仅用于系统级能力，如重启DSH进程、打开工作目录、开机自启配置、托盘控制，不参与业务数据通信

## 四、SSE 核心开发规范（React 专属）

### 4.1 基础连接配置

统一连接地址：`http://127.0.0.1:XXXX/api/agent/stream`（端口为DSH本地启动固定端口）

请求头固定配置：

- Content-Type: text/event-stream

- Cache-Control: no-cache

- Connection: keep-alive

### 4.2 SSE 事件类型（前端必须全量监听）

DSH官方标准化事件，前端需分类渲染、处理状态：

- **turn_start**：新一轮Agent会话开始

- **step_start**：单次模型推理/工具调用开始

- **tool_call**：工具调用详情（命令、文件路径、参数）

- **tool_result**：工具执行返回结果

- **stream_chunk**：模型流式输出文本

- **task_done**：整任务执行完成，携带耗时、token消耗、状态

- **error**：运行时报错、权限拦截、模型请求失败

### 4.3 React 最佳实践封装

采用 **useRef 持久化SSE实例 + useEffect 自动订阅/销毁**，避免组件重渲染重复创建连接。

核心逻辑：

- 页面挂载 / 会话切换：自动建立SSE连接

- 页面隐藏/窗口最小化：保持连接（适配后台长任务）

- 组件卸载/会话关闭：主动close连接，释放资源

- 连接断开：原生自动重连，重连后自动回溯历史事件，补全日志

### 4.4 边界异常处理（桌面端关键规范）

- **DSH进程重启**：SSE短暂断连后自动重连，前端保留当前会话状态，不清空页面数据

- **端口占用/启动失败**：前端捕获错误，展示“服务启动中”loading，自动轮询重试

- **网络波动/API密钥失效**：捕获error事件，弹窗提示，保留任务草稿

- **窗口关闭后台驻留**：SSE后台持续运行，重新打开窗口自动同步最新任务状态

## 六、React前端功能开发适配清单

### 6.1 必须基于SSE实现的功能

- Agent任务实时进度条、步骤日志滚动

- 工具调用记录实时刷新（文件读写、Shell命令、代码编辑）

- 模型输出流式打字机效果

- 任务成功/失败状态实时更新

- 后台任务进度无感同步（窗口最小化后重新唤醒）

### 6.2 基于HTTP短请求实现的功能

- 新建会话、切换会话、删除会话

- 提交Agent任务指令

- 配置模型参数、API密钥、代理、超时时间

- 导出任务日志、清空历史记录

### 6.3 基于Tauri IPC实现的系统功能

- 重启/停止DSH后台服务

- 打开本地工作目录、日志目录

- 开机自启、托盘图标、全局快捷键配置

- 应用版本检测、自动更新触发

## 七、性能优化规范（桌面端专属）

- **单会话单SSE连接**：全局唯一长连接，禁止多页面多实例重复创建

- **事件节流渲染**：高频stream_chunk事件做10ms节流，避免页面卡顿

- **后台任务缓存**：窗口失焦时暂停DOM渲染，持续监听SSE事件，聚焦后批量更新

- **自动资源释放**：长期闲置会话自动休眠SSE，切换会话重建连接

## 八、开发总结（最终落地口诀）

**业务流式推送全部SSE，一次性交互全部HTTP，系统级操作全部Tauri IPC**。

该方案完全适配DSH Cordis事件驱动架构、桌面后台驻留特性，兼顾稳定性、开发效率、官方版本兼容性，是React前端开发的唯一标准落地方案。

